{% from "govuk/components/select/macro.njk" import govukSelect %}
{% from "govuk/components/input/macro.njk" import govukInput %}

{% macro bankAccountRow(bankAccount, bankAccountDropDownItems, index) %}
  <div id="row-container">
    <div class="multiple-row">
      <div class="govuk-grid-column-one-third govuk-!-display-inline">
        {{ govukSelect({
          name: "rows[rows"+index+"][typeOfAccount]",
          items: bankAccountDropDownItems
        }) }}
      </div>
      <div class="govuk-grid-column-one-third govuk-!-display-inline">
        {{ govukSelect({
          name: "rows[rows"+index+"][joint]",
          items: [
            {
              value: "",
              text: "Choose"
            },
            {
              value: true,
              text: "Yes"
            },
            {
              value: false,
              text: "No"
            }
          ]
        }) }}
      </div>
      <div class="govuk-grid-column-one-third govuk-!-display-inline">
        {{ govukInput({
          name: "rows[rows"+index+"][balance]",
          value: bankAccount.balance,
          prefix: {
            text: "£"
          },
          classes: "govuk-input--width-5",
          spellcheck: false
        }) }}
      </div>
    </div>
  </div>
{% endmacro %}
