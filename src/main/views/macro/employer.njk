{% from "govuk/components/input/macro.njk" import govukInput %}

{% macro employerRow(employer, index, errorFor,t) %}
  <div class="row-container govuk-grid-column-two-thirds">
    <div class="multiple-row">
      <div class="govuk-form-group {%- if errors.length>0 %} govuk-form-group--error{% endif %}">
        <div class="govuk-grid-column-one-third govuk-!-display-inline">
          {{ govukInput({
              id: 'rows[' + index + '][employerName]',
              name: 'rows[' + index + '][employerName]',
              value: employer.employerName,
              hint: { text: t('PAGES.EMPLOYER.EMPLOYER_NAME') },
              label: {
                text: t('PAGES.EMPLOYER.EMPLOYER_NAME'),
                classes: 'govuk-visually-hidden'
              },
              errorMessage: { text: t(errorFor('rows[' + index + '][employerName]')) } if errorFor('rows[' + index + '][employerName]') else ''
            }) }}
          {{ govukInput({
              id: 'rows[' + index + '][jobTitle]',
              name: 'rows[' + index + '][jobTitle]',
              value: employer.jobTitle,
              hint: { text: t('COMMON.JOB_TITLE') },
              label: {
                text: t('COMMON.JOB_TITLE'),
                classes: 'govuk-visually-hidden'
              },
              errorMessage: { text: t(errorFor('rows[' + index + '][jobTitle]')) } if errorFor('rows[' + index + '][jobTitle]') else ''
            }) }}
        </div>
      </div>
    </div>
  </div>
{% endmacro %}
