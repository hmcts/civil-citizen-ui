{% from "govuk/components/radios/macro.njk" import govukRadios %}

{% macro yesNoRadioButton(t,name,form,checked) %}
  {% set errors = form.getErrors() %}
  {% set checked = checked.disability.option %}
              {{ govukRadios({
              idPrefix: "disability",
              name: "disability",
              fieldset: {
                legend: {
                  text: name,
                  isPageHeading: true,
                  classes: "govuk-fieldset__legend--l"
                }
              },
              items: [
                {
                  value: YesNo.YES,
                  checked: true if checked=="yes" else false,
                  html: "<span>" + t('Yes') + "</span>"
                },
                {
                  value: YesNo.NO,
                  checked: true if checked=="no" else false,
                  html: "<span>" + t('No') + "</span>"
                }
              ],
                errorMessage: {
                  html: t(errors[0].text)
                } if errors
            }) }}
{% endmacro %}
