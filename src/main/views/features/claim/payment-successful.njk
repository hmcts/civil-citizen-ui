{% extends "claim-details-tpl-dashboard.njk" %}
{% from "govuk/components/panel/macro.njk" import govukPanel %}
{% from "../../macro/csrf.njk" import csrfProtection %}
{% from "../dashboard/item-content.njk" import itemContent %}

{% block pageTitle %}
  {{ setPageTitle(t, pageTitle) }}
{% endblock %}

{% block nestedContent %}
          {{ csrfProtection(csrf) }}
          {% for content in paymentSuccessfulPanel %}
            {{ itemContent(content,t) }}
          {% endfor %}
          {% for content in paymentSuccessfulBody %}
            {{ itemContent(content,t) }}
          {% endfor %}
          <div class="govuk-width-container govuk-!-padding-top-5">
          {% for content in paymentSuccessfulButton %}
            {{ itemContent(content,t) }}
          {% endfor %}
          </div>
{% endblock %}
