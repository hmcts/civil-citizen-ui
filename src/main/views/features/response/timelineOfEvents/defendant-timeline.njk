{% extends "claim-details-tpl.njk" %}
{% from "govuk/components/button/macro.njk" import govukButton %}
{% from "../../../macro/contact-us-for-help.njk" import contactUsForHelp %}
{% from "../../../macro/page-title.njk" import setPageTitle %}
{% from "../../../macro/timeline-row.njk" import timelineRow %}
{% from "../../../macro/csrf.njk" import csrfProtection %}
{% set title = 'Add your timeline of events' %}
{% block pageTitle %}
  {{ setPageTitle(title) }}
{% endblock %}
{% block content %}
  <div class="govuk-width-container">
    <main class="govuk-main-wrapper" role="main">
      <div class="govuk-grid-row">
        <div class="govuk-grid-column-two-thirds">
          <h1 class="govuk-heading-l govuk-!-margin-bottom-1">{{ t(title) }}</h1>
          <h2 class="govuk-heading-s">{{ t("Add your timeline of events (optional)") }}</h2>
          <p class="govuk-body">{{ t('Include dates if you know them.') }}</p>
        </div>
      </div>
      <div class="govuk-grid-row">
        <form method="post" novalidate>
          {{ csrfProtection(csrf) }}
          <div class="govuk-grid-column-one-third govuk-!-display-inline">
            <label class="govuk-label--s">
              {{ t('Date') }}
              <span
                class="govuk-hint">{{ t('For example, {{ oneMonthAgo }}', { oneMonthAgo: 'now' | addDays(-30) | date }) }}</span>
            </label>
          </div>
          <div class="govuk-grid-column-one-third govuk-!-display-inline">
            <label class="govuk-label--s">{{ t('Description') }}
              <span class="govuk-hint">{{ t('For example, you might have signed a contract') }}</span>
            </label>
          </div>
        </form>
      </div>
    </main>
  </div>
{% endblock %}
