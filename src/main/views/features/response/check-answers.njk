{% extends "claim-details-tpl.njk" %}
{% from "govuk/components/summary-list/macro.njk" import govukSummaryList %}
{% from "govuk/components/button/macro.njk" import govukButton %}
{% from "../../macro/statement-of-truth.njk" import statementOfTruth %}

{% block content %}
  <div class="govuk-width-container">
    <main class="govuk-main-wrapper" role="main">
      <div class="govuk-grid-row">

        <br class="govuk-grid-column-two-thirds">

        <h1 class="govuk-heading-l">{{ t('PAGES.CHECK_YOUR_ANSWER.TITLE') }}</h1>

        {% for section in summarySections.sections %}
          <h2 class="govuk-heading-m govuk-!-margin-bottom-2">{{ section.title }}</h2>
          <div class="govuk-form-group">
            {{ govukSummaryList({
              rows: section.summaryList.rows
            }) }}
          </div>
        {% endfor %}

        {{ statementOfTruth(
          form,
          statementOfTruthType = SignatureType.RESPONSE,
          labelHtml = 'I believe that the facts stated in this response are true.
          <p>I understand that proceedings for contempt of court may be brought against anyone who makes, or causes to
          be made, a false statement in a document verified by a statement of truth without an honest belief in its
          truth.</p>',
          isQualified = (signatureType === SignatureType.QUALIFIED)
        ) }}

        <div class="govuk-form-group">
          <form method="post">
            {{ govukButton({
              text: t('Submit response')
            }) }}
          </form>
        </div>

      </div>
    </main>
  </div>

{% endblock %}
