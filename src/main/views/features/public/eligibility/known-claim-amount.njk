{% extends "claim-details-tpl.njk" %}
{% from "../../../macro/button.njk" import addButton %}
{% from "../../../macro/contact-us-for-help.njk" import contactUsForHelp with context %}
{% from "../../../macro/csrf.njk" import csrfProtection %}
{% from "../../../macro/yesNoGenericForm.njk" import yesNoRadioButton %}
{% from "../../../macro/page-title.njk" import setPageTitle %}
{% from "../../../macro/error-summary.njk" import errorSummary %}

{% block pageTitle %}
  {{ setPageTitle(t, pageTitle) }}
{% endblock %}
{% block content %}
  <div class="govuk-width-container">
    <main class="govuk-main-wrapper" role="main">
      <div class="govuk-grid-row">
        {% if form.hasErrors() %}
          {{ errorSummary(form.getErrors(), t) }}
        {% endif %}
      </div>
      <div class="govuk-grid-row">
        <div class="govuk-grid-column-two-thirds">
          <h1 class="govuk-heading-l" id="page-heading">
            {{t('PAGES.ELIGIBILITY_KNOWN_CLAIM_AMOUNT.TITLE')}}
          </h1>
          <form method="post" novalidate aria-labelledby="page-heading">
            {{ csrfProtection(csrf) }}

            <fieldset class="govuk-fieldset">
              <legend class="govuk-fieldset__legend govuk-fieldset__legend--m">
                {{t('PAGES.ELIGIBILITY_KNOWN_CLAIM_AMOUNT.QUESTION')}}
              </legend>
              {{ yesNoRadioButton(t, 'option', 'option', '', form, '', {
                classes: 'govuk-radios--inline'
              }) }}
            </fieldset>

            <div class="govuk-form-group">
              {{ addButton(t, 'SAVE_AND_CONTINUE_CARM', { type: 'submit' }) }}
              {{ contactUsForHelp(t) }}
            </div>
          </form>
        </div>
      </div>
    </main>
  </div>
{% endblock %}
