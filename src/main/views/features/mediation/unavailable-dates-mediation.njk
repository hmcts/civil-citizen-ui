{% extends "claim-details-tpl.njk" %}
{% from "../../macro/csrf.njk" import csrfProtection %}
{% from "../../macro/contact-us-for-help.njk" import contactUsForHelp with context %}
{% from "../../macro/page-title.njk" import setPageTitle %}
{% from "../../macro/unavailable-date-row.njk" import unavailableDateRow %}
{% from "../../macro/error-summary.njk" import errorSummary %}
{% from "govuk/components/button/macro.njk" import govukButton %}

{% block pageTitle %}
{{ setPageTitle(t, pageTitle) }}
{% endblock %}

{% set rowNumberToShowRemoveButton = 2 %}
{% set isRemoveButtonHidden = form.model.items.length < rowNumberToShowRemoveButton %}
{% set itemsErrors = form.errorFor('items') %}

{% block content %}

{% if form.hasErrors() %}
{{ errorSummary(form.getAllErrors(), t) }}
{% endif %}

<div class="govuk-width-container">
    <main class="govuk-main-wrapper" role="main">
        <div class="govuk-grid-row">
            <div class="govuk-grid-column-two-thirds">
                <h1 class="govuk-heading-l">{{ pageText }}</h1>

                <div id="hint1" class="govuk-hint">
                    {{pageHint.text1}}
                </div>

                <div id="hint2" class="govuk-hint">
                    {{pageHint.text2}}
                </div>

                <form method="post" novalidate>
                    {{ csrfProtection(csrf) }}

                    <fieldset class="govuk-fieldset">
                        <legend class="govuk-visually-hidden">{{ pageText }}</legend>

                    {% for unavailableDate in form.model.items %}
                      {{ unavailableDateRow(unavailableDate, 'PAGES.UNAVAILABILITY_SELECT_DATES_MEDIATION.PAGE_TEXT', loop.index0, form.errorFor, t, {hidden:isRemoveButtonHidden}, today, true) }}
                    {% else %}
                      {{ unavailableDateRow(unavailableDate, 'PAGES.UNAVAILABILITY_SELECT_DATES_MEDIATION.PAGE_TEXT', 0, form.errorFor, t, {hidden:true}, today, true) }}
                    {% endfor %}
                    </fieldset>

                    <div class="govuk-!-margin-top-6">
                        {{ govukButton({
                                           text: t('PAGES.UNAVAILABLE_DATES_FOR_HEARING.ADD_ANOTHER_DATE'),
                            classes: "govuk-button--secondary append-row"
                        }) }}
                    </div>
                    <div class="govuk-form-group">
                        {{ govukButton({
                                           text: t('COMMON.BUTTONS.SAVE_AND_CONTINUE_CARM')
                        }) }}
                    </div>
                </form>

                {{ contactUsForHelp(t, '', '', isCarm) }}
            </div>
        </div>
    </main>
</div>

{% endblock %}
