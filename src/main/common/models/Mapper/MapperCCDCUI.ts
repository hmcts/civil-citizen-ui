import elements = CodeceptJS.browserCodecept.elements;

const configObjectCCDToCUI ={
  'defenceAdmitPartPaymentTimeRouteRequired': 'Claim.Respondent1.ResponseType',
};

class MapperToCui {

/*  mapToCUI(){

  }
  recursivelyIterateProperties() {
    for (const prop in Object.keys(configObjectCCDToCUI)) {
      console.log(prop);
      if (!(typeof(jsonObject[prop]) === 'string')

        && !(jsonObject[prop] instanceof Array)) {
        this.recursivelyIterateProperties(jsonObject[prop]);
      }

    }
  }*/
}
