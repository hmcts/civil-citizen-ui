# Reduced-stack WireMock contracts

## Ownership and scope

The mappings under `charts/civil-citizen-ui/wiremock` are the authoritative test doubles for the reduced-stack CUI create-claim PoC and first migration batch. They are deliberately consumer-owned because they are packaged into the CUI preview chart and exercise a CUI-only Jenkins journey. The central `civil-wiremock-mappings` repository remains authoritative for shared full-stack journeys; these reduced-stack assets must not be copied there unless ownership is explicitly transferred.

The inventory was derived from `CivilServiceClient`, the create-claim controllers and `src/test/functionalTests/tests/mocked/createClaim_tests.js`, then compared with the central CUI mappings. The central set has broad legacy rules and still maps total calculation at `/fees/claim/total-claim-amount`; it has no dashboard scenario-creation rule. The former local bundle also used broad claim, role and event regexes and returned an incomplete create-claim response inline.

## Endpoint inventory and gap analysis

| Scenario                                        | CUI client / method                  | HTTP | Exact path template                                                             | Query              | Significant headers                                                                     | Request schema / example                       | Expected | Minimum response consumed                                                | Fixture                                | Mapping                          | Owner | Pact                                                                                                                                           |
| ----------------------------------------------- | ------------------------------------ | ---- | ------------------------------------------------------------------------------- | ------------------ | --------------------------------------------------------------------------------------- | ---------------------------------------------- | -------- | ------------------------------------------------------------------------ | -------------------------------------- | -------------------------------- | ----- | ---------------------------------------------------------------------------------------------------------------------------------------------- |
| Start draft                                     | `CivilServiceClient.createDashboard` | POST | `/dashboard/scenarios/Scenario.AAA6.ClaimIssue.ClaimSubmit.Required/{userId}` | None               | `Content-Type: application/json`; auth headers are supplied but not behaviour-selecting | `{"params":{}}`                                | 200      | No fields                                                                | `create-claim-dashboard-scenario.json` | `create-claim-dashboard.json`    | CUI   | Covered; see dashboard task and notification coverage below                                                                                    |
| Calculate total (dormant; no production caller) | `calculateClaimTotalAmount`          | POST | `/fees/claim/total-amount`                                                      | None               | `Content-Type: application/json`                                                        | `ClaimUpdate`, with numeric `totalClaimAmount` | 200      | JSON number                                                              | `create-claim-total-amount.json`       | `create-claim-total-amount.json` | CUI   | Dormant/uncovered; no new Pact planned                                                                                                         |
| Claim fee                                       | `getClaimFeeData`                    | GET  | `/fees/claim/{amount}`                                                          | None               | Civil Service auth                                                                      | None                                           | 200      | `calculatedAmountInPence`, `code`, `version`                             | `create-claim-claim-fee.json`          | `create-claim-fees.json`         | CUI   | Covered; amount, code and version protected by the existing claim-fee interaction                                                              |
| Hearing fee                                     | `getHearingAmount`                   | GET  | `/fees/hearing/{amount}`                                                        | None               | Civil Service auth                                                                      | None                                           | 200      | `calculatedAmountInPence`, `code`, `version`                             | `create-claim-hearing-fee.json`        | `create-claim-fees.json`         | CUI   | Covered; see fees and calculation coverage below                                                                                               |
| Submit draft                                    | `submitDraftClaim` / `submitEvent`   | POST | `/cases/draft/citizen/{submitterId}/event`                                      | None               | `Content-Type: application/json`; Civil Service auth                                    | `EventDto`; `event=CREATE_LIP_CLAIM`           | 200      | `id`, `state`, `last_modified`, mandatory `case_data` used by conversion | `create-claim-submitted-claim.json`    | `create-claim-submit.json`       | CUI   | Four translated party-pairing interactions and matching provider states; see [Pact inventory](../src/test/contract/interaction-inventory.json) |
| Read submitted claim                            | `retrieveClaimDetails`               | GET  | `/cases/1111222233334444`                                                       | None               | Civil Service auth                                                                      | None                                           | 200      | Same representative case fields as submit                                | `create-claim-submitted-claim.json`    | `create-claim-read.json`         | CUI   | Consumer interactions and provider states added; see case-access coverage below                                                                |
| Resolve case roles                              | `getUserCaseRoles`                   | GET  | `/cases/1111222233334444/userCaseRoles`                                         | None               | Civil Service auth                                                                      | None                                           | 200      | Role string array                                                        | `create-claim-user-case-roles.json`    | `create-claim-read.json`         | CUI   | Consumer interactions and provider states added; see case-access coverage below                                                                |
| Find address                                    | Ordnance Survey postcode client      | GET  | `/search/places/v1/postcode`                                                    | `postcode=MK5 7HH` | API key is supplied but not behaviour-selecting                                         | None                                           | 200      | `results[].DPA` address fields                                           | `create-claim-address-search.json`     | `create-claim-address.json`      | CUI   | External schema checks tracked in DTSCCI-6423; no Civil Service Pact                                                                           |

Before/after route corrections: dashboard changed from `/dashboard/scenarios/Scenario\\.AAA6\\.ClaimIssue\\.ClaimSubmit\\.Required/.*` to the fixed scenario plus one non-empty redis-key segment and an exact body; total calculation changed from central `/fees/claim/total-claim-amount` to the client contract `/fees/claim/total-amount` with a numeric `totalClaimAmount` match.

## Updating contracts

When a Civil Service contract changes, update the CUI client and its unit/Pact tests first, then update this inventory, mapping and minimal synthetic fixture in the same PR. Run `yarn wiremock:validate` and `yarn test:wiremock-contracts`. The validator rejects invalid JSON, missing fixtures, duplicate/conflicting rules, missing priorities and generic catch-alls. The contract test starts the complete set and proves every selected request matches while important invalid requests remain 404. Keep identifiers stable and synthetic; do not add production data or volatile request matching.

## Draft-submission and validation-error Pact coverage

`CivilServiceCreateClaim.test.ts` exercises `submitDraftClaim` with the real `translateDraftClaimToCCDR2` payload and `submitEvent` with the real error parser. The [Pact inventory](../src/test/contract/interaction-inventory.json) lists exact interaction and provider-state names.

| HTTP boundary                              | Consumer examples                                                                                                                                     | Provider states                                                                                                           |
| ------------------------------------------ | ----------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------- |
| `POST /cases/draft/citizen/{userId}/event` | Individual/company, company/organisation, organisation/sole trader and sole trader/individual; required translated fields and realistic case response | `A draft {party-pair} claim can be submitted`                                                                             |
| `POST /cases/{id}/citizen/{userId}/event`  | 422 callback messages and warnings; 422 field-error messages; 422 without actionable fields preserving Axios-error propagation                        | `Citizen event submission returns {callback errors and warnings/field validation errors/no actionable validation fields}` |

Civil Service states use its real exception advice to produce the validation responses. These interactions protect the production draft path, rather than the former empty payload sent to a numeric case ID.

## Case-access Pact coverage

`CivilServiceCaseAccess.test.ts` calls the real HTTP clients and checks converted `Claim` and dashboard item values. Exact interaction descriptions and provider-state names are maintained in the [Pact inventory](../src/test/contract/interaction-inventory.json); `CivilCitizenUiProviderSupport` in Civil Service supplies the states using real controllers, runtime Jackson configuration and exception advice.

| Client method / HTTP boundary                                                                            | Consumer examples                                                                                                                                                | Provider states                                                                                     |
| -------------------------------------------------------------------------------------------------------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------- | --------------------------------------------------------------------------------------------------- |
| `retrieveClaimDetails`: `GET /cases/{id}` and `GET /cases/{id}/userCaseRoles`                            | Fresh request and empty role cache; claimant, defendant and empty roles; identity, parties, amount, state, last modification and business-process polling values | `Case details and {claimant/defendant/no roles} access are available` (two interactions per state)  |
| `retrieveClaimDetails`: `GET /cases/{id}`                                                                | Provider 404 propagates as an Axios error                                                                                                                        | `The requested case does not exist`                                                                 |
| `getClaimsForClaimant` / `getClaimsForDefendant`: `GET /cases/{claimant/defendant}/{userId}?page={page}` | Default page 1 with CUI and OCMC items; explicit page 2 with empty results; numeric totalPages, string-serialized amount, status and derived links               | `The {claimant/defendant} dashboard page {1/2} is available`                                        |
| `verifyPin`: `POST /assignment/reference/{caseReference}`                                                | Public JSON PIN; converted case on 200; mismatched PIN 400 and missing reference 401 propagate                                                                   | `A Civil reference and PIN are valid`; `Civil PIN validation fails for {invalid PIN/missing claim}` |
| `verifyOcmcPin`: `POST /assignment/reference/{caseReference}/ocmc`                                       | Public JSON PIN; redirect string with application/json on 200; invalid PIN 400 rejects instead of redirecting                                                    | `An OCMC reference and PIN are valid`; `An OCMC PIN is invalid`                                     |
| `isDefendantLinked`: `GET /assignment/reference/{caseReference}/defendant-link-status`                   | JSON true/false; missing search result is false; upstream 404 becomes false; provider 500 propagates                                                             | `Defendant link status is {linked/unlinked/no search result/upstream not found/provider failure}`   |
| `assignDefendantToClaim`: `POST /assignment/case/{claimId}/DEFENDANT`                                    | Bearer auth and JSON PIN; 200 with no body; rejected PIN 400 propagates                                                                                          | `Defendant assignment is {accepted/rejected PIN}`                                                   |

The provider dashboard DTO has no URL field: CUI builds links from claimId, ocmc and the item model's route. The OCMC PIN controller returns 200 despite its Swagger 302 annotation. Missing Civil PIN references raise the existing exception mapped to 401; neither PIN endpoint has a normal empty-result success path to invent a contract for. Cache mechanics remain covered by unit tests.

The successful OCMC response is an unquoted URL labelled application/json. Its provider state enables Pact's [content-type override](https://docs.pact.io/implementation_guides/jvm/provider/maven) for that interaction only, comparing exact text while checking the unchanged header. JSON diff formatting is disabled for the same interaction because it attempts to parse the raw URL. Both properties are restored afterwards, and the CUI verifier is isolated from concurrent tests so the override cannot affect other JSON contracts.

These are test protections, with no production behaviour changes.


## Fees and calculation Pact coverage

`CivilServiceCalculations.test.ts` calls the real Civil Service HTTP clients. Interest inputs use the translator called by `interestUtils`; repayment inputs use `toCCDClaimantProposedPlan`. Independent request examples are stored in `calculations.json` and mirrored in the provider's `civil-cui-calculations.json`. The existing claim-fee interaction is extended rather than duplicated. Exact interaction names and provider states are recorded in the [Pact inventory](../src/test/contract/interaction-inventory.json).

| Client / HTTP boundary                                                                        | Consumed fields and examples                                                                                                                                    | Provider states                                                                              |
| --------------------------------------------------------------------------------------------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------- | -------------------------------------------------------------------------------------------- |
| `getClaimFeeData`: `GET /fees/claim/{amount}`                                                 | String amount in pence converted to number; code and string version                                                                                             | `A claim issue fee is available for a claim amount of 1000`                                  |
| `calculateClaimInterest`: `POST /fees/claim/calculate-interest`                               | Translated statutory rate, different rate with zero interest, and breakdown amount; date, rate and amount fields; numeric result                                | `Claim interest {statutory/different rate zero/breakdown} can be calculated`                 |
| `getHearingAmount`: `GET /fees/hearing/{amount}`                                              | String amount in pence used to render pounds; code and version                                                                                                  | `A hearing fee is available for a claim amount of 1000`                                      |
| `getFeeRanges`: `GET /fees/ranges`                                                            | Numeric min_range/max_range; flat_amount.amount and percentage_amount.percentage; null maximum for an open-ended range; conversion to FeeRange and table values | `Flat and percentage fee ranges are available`                                               |
| `getGeneralApplicationFee`: `POST /fees/general-application`                                  | Supported applicationTypes, consent and notice flags, null or adjourn-hearing date; string fee amount, code and version                                         | `A General Application fee for {consent/notice/without notice/adjourn hearing} is available` |
| `calculateExtendedResponseDeadline`: `POST /cases/response/deadline`                          | Serialised responseDate and plusDays 0/5; ISO date-string result, including the client's unchanged runtime string                                               | `A response deadline with {0/5} extra days can be calculated`                                |
| `getAgreedDeadlineResponseDate`: `GET /cases/response/agreeddeadline/{claimId}`               | ISO date converted to Date; provider absence is 200 with no body and resolves undefined                                                                         | `An agreed response deadline is {present/absent}`                                            |
| `getCalculatedDecisionOnClaimantProposedRepaymentPlan`: `POST /cases/{claimId}/courtDecision` | Translated instalment amount in pence, monthly frequency and first payment date; both exact decision enum strings                                               | `The repayment decision is {IN_FAVOUR_OF_CLAIMANT/IN_FAVOUR_OF_DEFENDANT}`                   |

Provider states use real Fee, Fee2Dto, CaseData and repayment models, controllers, runtime JSON configuration and advice. Calculation services return deterministic examples: arithmetic, tariff selection, rounding and cache mechanics remain unit-test responsibilities. The extended-deadline client's Date assertion is only a TypeScript cast; the contract deliberately checks its actual string result without changing application behaviour.

Civil Service suites all generate Pact specification V4 so JSON scalar date and decision strings retain their wire representation. Artifact validation rejects a V2/V3 downgrade when suites merge into the shared file; IDAM and S2S contracts retain their existing format.


## Dashboard task and notification Pact coverage

`CivilServiceDashboard.test.ts` exercises the real clients with a fresh deterministic session. `dashboard.json` supplies bilingual examples mirrored in the provider's `civil-cui-dashboard.json`; provider states use the real DashboardController, DTOs, runtime serializers and exception advice. Exact interaction names and provider states are listed in the [Pact inventory](../src/test/contract/interaction-inventory.json).

| Client / HTTP boundary                                                                                | Consumed fields and examples                                                                                                                                                       | Provider states                                                                                             |
| ----------------------------------------------------------------------------------------------------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ----------------------------------------------------------------------------------------------------------- |
| `retrieveDashboard`: `GET /dashboard/taskList/{caseId}/role/{role}`                                   | CLAIMANT/DEFENDANT; populated and empty lists; bilingual categories, task names, statuses and hints; category grouping and status colours                                          | `The {CLAIMANT/DEFENDANT} dashboard task list is {populated/empty}`                                         |
| `retrieveNotification`: `GET /dashboard/notifications/{caseId}/role/{role}`                           | CLAIMANT/DEFENDANT; bilingual titles and descriptions, links, params, created/deadline dates, Click/Session TTL, null or populated actions; values retained after client filtering | `The {CLAIMANT/DEFENDANT} Civil notifications are {populated/empty}`                                        |
| `retrieveGaNotification`: `GET /dashboard/notifications/ids/{commaSeparatedIds}/role/{role}`          | APPLICANT/RESPONDENT; two application IDs; conversion of keyed object to Map and notification lists; populated and empty arrays after filtering                                    | `The {APPLICANT/RESPONDENT} GA notifications are {populated/empty}`                                         |
| `createDashboard`: `POST /dashboard/scenarios/Scenario.AAA6.ClaimIssue.ClaimSubmit.Required/{userId}` | Bearer auth; Map-based params serialises as `{params:{}}`; 200 with no response body and undefined client return                                                                   | `A draft dashboard scenario can be created`                                                                 |
| `recordClick`: `PUT /dashboard/notifications/{notificationId}`                                        | Bearer auth, UUID and JSON null request body; 200 with no response body; malformed identifier rejected with 400                                                                    | `A dashboard notification update has a {valid/malformed} identifier`                                        |
| `updateTaskStatus`: `PUT /dashboard/taskList/{taskItemId}`                                            | Bearer auth, UUID and JSON null request body; 200 entity response ignored by the client; malformed identifier 400 and missing task 412 propagate                                   | `A dashboard task update has a {valid/malformed} identifier`; `The dashboard task to update does not exist` |

The GA notification service retains every requested ID even when its list is empty; the contract does not invent an empty-object response for two requested IDs. Task statuses are checked against the provider enum and its Welsh labels. The notification examples exercise consumed action fields through existing client filtering; exhaustive session rules and UI navigation remain in their existing tests. A missing task raises the existing IllegalArgumentException mapped to 412. Missing notification clicks are a no-op in the provider service, so no fabricated not-found rejection is added.

## Payments and Help with Fees Pact coverage

`CivilServiceFeePayment.test.ts` retains the existing claim-issue and GA creation/success interactions, with exact business-significant status values. `CivilServicePaymentsAndHelpWithFees.test.ts` adds hearing payments, non-final and failed statuses, missing payments, and both Help with Fees events. The [interaction inventory](../src/test/contract/interaction-inventory.json) lists every interaction and provider state.

| Client / HTTP boundary                                                                                                    | Consumed fields and examples                                                                                                                                                                             | Provider states                                                                                                              |
| ------------------------------------------------------------------------------------------------------------------------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ---------------------------------------------------------------------------------------------------------------------------- |
| `getFeePaymentRedirectInformation`: `POST /fees/HEARING/case/{claimId}/payment`                                           | Bearer auth, empty-string request; reference, Initiated status, redirect URL and runtime ISO date; creation does not populate paymentAmount                                                              | `A hearing payment can be initiated`                                                                                         |
| `getFeePaymentStatus`: `GET /fees/{feeType}/case/{claimId}/payment/{paymentReference}/status`                             | CLAIMISSUED and HEARING; exact Success, Failed, Initiated and Pending values; references, numeric amount, paymentFor, nullable redirect and failure details                                              | Existing claim success state; `The {CLAIMISSUED/HEARING} payment status is {Success/Failed/Initiated/Pending}` as applicable |
| `getGaFeePaymentStatus`: `GET /fees/case/{applicationId}/ga/payment/{paymentReference}/status`                            | Reuses GA success; adds Failed, Initiated and Pending; numeric amount, nullable redirect/paymentFor, cancellation error code and description                                                             | Existing GA success state; `The GA payment status is {Failed/Initiated/Pending}`                                             |
| Payment status clients: same GET routes                                                                                   | Missing upstream payment propagates the existing HTTP 500; consumer does not interpret it as a successful or pending payment                                                                             | `The {CLAIMISSUED/HEARING/GA} payment cannot be found upstream`                                                              |
| `triggerNotifyEvent` / `submitEvent`: `POST /cases/{claimId}/citizen/{userId}/event`                                      | Real hearing service translates reference to hwfFeeType HEARING and hearingHelpFeesReferenceNumber; exact APPLY_HELP_WITH_HEARING_FEE; converted claim identity, state, last-modified date and reference | `Hearing Help with Fees can be submitted`                                                                                    |
| `saveAndTriggerNotifyGaHwfEvent` / `GaServiceClient.submitEvent`: `POST /cases/{applicationId}/ga/citizen/{userId}/event` | Real service translation; exact NOTIFY_HELP_WITH_FEE and nested generalAppHelpWithFees; Yes with reference, No without reference; 200 response ignored by the service, 422 propagates                    | `GA Help with Fees submission is {Yes/No/rejected}`                                                                          |

Provider states use FeesPaymentController and CasesController with production serializers and exception advice. GA payment status does not populate paymentFor, unlike the generic fee endpoint; the previous manufactured GA value was removed. Status responses do not supply a redirect URL. Missing downstream payments are wrapped in PaymentsApiException and become HTTP 500 through existing advice, rather than a fabricated 404. The GA rejection state raises a downstream Feign validation exception so the real advice generates the response. The service checks isolate unused web-app startup and redirect only service URL configuration to Pact; HTTP clients, translators and response conversion remain real.

## Response, extension and Query Management Pact coverage

`CivilServiceResponses.test.ts` calls the real response clients with production translation output from deterministic claims in `responseClaims.ts`. `responseRequests.json` records the reviewed request shapes and is mirrored by the provider's `civil-cui-responses.json`. Response assertions cover consumed Claim values rather than assuming that request and response date or money types are identical. Exact interaction names and states are recorded in the [interaction inventory](../src/test/contract/interaction-inventory.json).

| Client / event at `/cases/{claimId}/citizen/{userId}/event`                     | Request variants and consumed response fields                                                                                                                                                                                                                                                             | Provider states                                                                                                         |
| ------------------------------------------------------------------------------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ----------------------------------------------------------------------------------------------------------------------- |
| `submitDefendantResponseEvent` / `DEFENDANT_RESPONSE_CUI`                       | Full defence with disputed text, DQ language and mediation; full admission with monthly instalments; part admission with amount owed and payment date. Asserts converted party, response type, repayment, language and mediation values.                                                                  | `The {defence/full-admission/part-admission} citizen response can be submitted`                                         |
| `submitClaimantResponseEvent` / `CLAIMANT_RESPONSE_CUI`                         | Acceptance, rejection with DQ/mediation, alternative instalments and set-date proposals. Protects Yes/No choices, repayment enums, amounts and dates; asserts converted acceptance, mediation and payment options.                                                                                        | `The {claimant-acceptance/claimant-rejection/claimant-instalments/claimant-set-date} citizen response can be submitted` |
| `submitAgreedResponseExtensionDateEvent` / `INFORM_AGREED_EXTENSION_DATE_SPEC`  | The calculated deadline is an ISO date string at runtime, despite the client Date annotation. Protects respondentSolicitor1AgreedDeadlineExtension and response language, plus returned identity, date and last-modified fields.                                                                          | `An agreed response extension can be submitted`                                                                         |
| `createQuery` / `submitQueryManagementRaiseQuery` / `queryManagementRaiseQuery` | Calls the real query service for a new query with a synthetic attachment and a follow-up retaining its parent. Protects queries/caseMessages, separate collection and message IDs, parentId, author, subject, body, hearing date and document links. Asserts returned query data after client conversion. | `A {new/follow-up} citizen query can be submitted`                                                                      |

Provider states match the complete translated event envelope and verify the exact CaseEventService call through CasesController. Returned responses use production CaseData, party, query and date models with runtime Jackson configuration. Query request timestamps and UUID generation are fixed only in tests; HTTP calls and query construction remain real. Cache/session mechanics, UI navigation and functional scenarios remain in their existing tests.

## Judgment and settlement Pact coverage

`CivilServiceJudgmentEvents.test.ts` exercises the real client wrappers for default judgment, judgment by admission, settlement, signed settlement agreement and judgment paid in full. The interactions protect the distinct event names and consumed payment, settlement, signature and paid-in-full fields; workflow eligibility and judgment calculations remain outside Pact coverage. Provider states use the production citizen event controller and return converted Claim identity, state and last-modified values.

## Document and case progression Pact coverage

`CivilServiceDocumentsAndProgression.test.ts` exercises the actual upload/download clients and case-event client. Upload uses the document-configured Axios instance and a real multipart `FormData` request with the `file` part, filename, bearer token and deterministic binary bytes; its response is binary JSON and is decoded into the consumed `CaseDocument` links. Download checks returned bytes, content type and original filename through the same document-configured client, and the missing-document path preserves the provider's 404 as an Axios error. Evidence requests use `toCCDEvidenceUpload`; mediation documents use the production document mapper and CCD collection models; trial-readiness and reconsideration bodies use their production translators.

| Client / event boundary | Request shape and consumed response | Provider state |
| --- | --- | --- |
| `uploadDocument`: `POST /case/document/generateAnyDoc` | Multipart `file` part with filename and binary content; binary JSON response decoded to document URL, binary URL, filename and document size | `A synthetic citizen document can be uploaded` |
| `retrieveDocument`: `GET /case/document/downloadDocument/{documentId}` | Binary bytes, `Content-Type` and `original-file-name` become a `FileResponse`; 404 remains an Axios error | `A synthetic document exists for download`; `The requested document does not exist` |
| `submitEvent`: `EVIDENCE_UPLOAD_APPLICANT` / `EVIDENCE_UPLOAD_RESPONDENT` | Production evidence translator output with collection ID, witness metadata, document links and upload date; converted Claim identity/state | `A translated {EVIDENCE_UPLOAD_APPLICANT/EVIDENCE_UPLOAD_RESPONDENT} event can be submitted` |
| `submitEvent`: `CUI_UPLOAD_MEDIATION_DOCUMENTS` | Production-mapped referred document collection with category, synthetic link, document type and document/upload dates; converted Claim identity/state | `A CUI mediation document submission can be made` |
| `submitTrialArrangement`: `TRIAL_READINESS` | Production trial-arrangement translator output for readiness and hearing requirements/comments; converted Claim identity/state | `A translated trial readiness response can be submitted` |
| `submitRequestForReconsideration`: `REQUEST_FOR_RECONSIDERATION` | Production translation for the initial defendant request and claimant comments response; converted Claim identity/state | `A translated reconsideration {initial request/comments response} can be submitted` |

## General Application Pact coverage

`CivilServiceGeneralApplications.test.ts` exercises the deployed CUI-to-Civil-Service boundary with the real GA clients and production translators. It protects ordinary and COSC initiation, GA case retrieval, parent-case application lists, ordinary and urgent responses, and the four distinct judge/additional-document event collections. Initiation checks the parent-case link returned for the next CUI journey; GA response checks the request because the current response converter intentionally returns an empty `Application`. Parent-case list examples arrive out of creation-date order so the client sort is asserted, including an empty result.

| Client / event | Request shape and consumed response | Provider state |
| --- | --- | --- |
| `submitInitiateGeneralApplicationEvent`: `INITIATE_GENERAL_APPLICATION` | Production translation for an adjourn-hearing application; protects type, respondent agreement, order/reasons, notice, hearing details, statement of truth and evidence links; checks returned GA ID and parent-case link | `An ordinary GA can be initiated` |
| `submitInitiateGeneralApplicationEventForCosc`: `INITIATE_GENERAL_APPLICATION_COSC` | Production certificate-of-satisfaction translation; protects application type, final payment date, payment evidence, statement of truth and evidence; checks returned GA ID and parent-case link | `A COSC GA can be initiated` |
| `GaServiceClient.getApplication`: `GET /cases/{applicationId}` | Case identity, state, created/modified dates, GA type, fee, response offer, parent link and document fields | `A General Application case exists` |
| `GaServiceClient.getApplicationsByCaseId`: `GET /cases/{parentId}/ga/applications` | Populated and empty `cases` arrays; multiple case IDs and dates verify ascending creation-date sorting and consumed GA type, fee and parent link | `General Applications for parent case are {populated/empty}` |
| `submitRespondToApplicationEvent` / `RESPOND_TO_APPLICATION`; `submitRespondToApplicationEventForUrgent` / `RESPOND_TO_APPLICATION_URGENT_LIP` | Production response translation protects hearing details, debtor offer, consent/reason, statement of truth and document links; asserts the actual empty-`Application` client result | `An {ordinary/urgent} GA response can be submitted` |
| `GaServiceClient.submitEvent`: `UPLOAD_ADDL_DOCUMENTS`, `RESPOND_TO_JUDGE_DIRECTIONS`, `RESPOND_TO_JUDGE_ADDITIONAL_INFO`, `RESPOND_TO_JUDGE_WRITTEN_REPRESENTATION` | Distinct collection names and their document URL, binary URL and filename wrappers; the additional-information event also protects the submitted text | `additional documents can be submitted to a General Application`; `judge {directions/additional information/written representation} documents can be submitted to a General Application` |

The contracts target Civil Service because the deployed GA base URL maps to `CIVIL_SERVICE_URL`. Provider-state names and interaction descriptions are listed in the [interaction inventory](../src/test/contract/interaction-inventory.json). Document identifiers and links are synthetic; the upload binary transport remains covered by the separate document boundary contracts.
